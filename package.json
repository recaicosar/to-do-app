{
  "name": "to-do-app",
  "private": true,
  "scripts": {
    "setup": "lerna bootstrap",
    "dev": "lerna run dev",
    "watch": "lerna run dev --parallel",
    "client-dev":"lerna run dev --scope client",
    "storybook":"lerna run storybook --scope ui-kit"
  },
  "devDependencies": {
    "lerna": "^5.1.1"
  }
}
